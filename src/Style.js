import { StyleSheet} from 'react-native'

const Styles = StyleSheet.create({
    Shadow:{
        shadowColor:"black",
        shadowOpacity:0.5,
        elevation:35,
        shadowRadius:20,
        shadowOffset:{
            width:0,
            height:0
        }

    }
})

export default Styles